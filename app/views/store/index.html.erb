<p id="notice"><%= notice %></p>
<div class="container">
<br>
<div class="row">
					<% @items.each do |item| %>
                    <div class="col-sm-4 col-lg-4 col-md-4 ">
                        <div class="thumbnail thumbnail2 marginItem">
                        	<%= image_tag(item.img) %>
                            <div class="caption">
                                <h4 class="pull-right">Preço: <%= item.price %></h4>
                                <h4><%= item.name %>
                                </h4>
                                <p></p>
                            </div>
                            <div class="ratings">
                                <p>
                                    <span class="glyphicon glyphicon-star"></span>
                                    <span class="glyphicon glyphicon-star"></span>
                                    <span class="glyphicon glyphicon-star"></span>
                                    <span class="glyphicon glyphicon-star"></span>
                                    <span class="glyphicon glyphicon-star"></span>
                                </p>
                                <%= button_to 'Comprar', line_items_path(item_id: item) %>
                            </div>
                        </div>
                    </div>

                
		<% end %>
                </div>
</div>